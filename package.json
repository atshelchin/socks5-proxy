{
  "name": "socks5-proxy",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "test": "bun test",
    "bench": "bun run src/bench.ts",
    "build": "bun build --compile --target=bun-linux-x64 index.ts --outfile socks5-proxy-linux && bun build --compile --target=bun-linux-x64-baseline index.ts --outfile socks5-proxy-linux-baseline && echo 'Built: socks5-proxy-linux + socks5-proxy-linux-baseline'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
